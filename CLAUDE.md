# 專案協作準則（AS400_SQL_Service_for_i）

## 計畫先行（Plan-first）

- 不管是新功能規劃、UI/UX優化計畫、bug修復計畫、資料查證計畫……只要是「計畫」性質的產出，動手實作前一律先產出一份 md 檔到 `plans/`（檔名格式 `YYYY-MM-DD-主題.md`），內容至少包含：問題/需求清單、已查明的根本原因（若有）、對應的修復或實作方向。使用者確認方向後才開始寫入程式碼。
- **這條規則要每次真的產出檔案去執行，不能只憑對話當下的記憶或判斷覺得「這次很單純不用寫」就跳過**——即使任務討論起來很簡單，也要先落地成檔案。
- 跟既有的 `progress/`、`_agent_logs/` 是互補關係，三者記錄的東西不同、不要混用：
  - `plans/` 記「要做什麼、為什麼、根本原因」——動手前的規劃。
  - `progress/` 記「做的當下發生了什麼、已知限制、本輪動作範圍」——執行過程。
  - `_agent_logs/` 記「停損機制觸發時的證據、回滾檢查點」——失敗處理。

## 不遺漏使用者提出的事項

- 使用者在同一則訊息或跨多輪對話中提出多件事時，不能只處理當下被直接問到/最新的那一件，其餘的事項要嘛一併處理，要嘛在回覆裡明確列出「這件事還沒處理」並問清楚是否要現在做——不能讓話題被後續對話帶過去就悄悄消失，事後假裝沒發生過。這條規則的起因：曾經一輪對話裡使用者同時提出兩件事，我只回了其中一件，另一件在使用者沒有明確答覆的情況下就被默默放掉，直到使用者回頭質問才發現漏掉，而不是Claude當下有主動追蹤或告知。
- 具體做法：
  1. 使用者提出的每一件待處理事項，都要記進 `TodoWrite` 待辦清單或 `plans/` 底下的規劃檔案裡，不能只靠對話當下的印象判斷「這個晚點再說」卻沒有真的留下記錄。
  2. 每次要結束一輪回覆前，检查是否有先前記錄下來、使用者還沒鬆口說可以不用做/已經確認做完的事項，如果有，要在回覆裡主動提及目前狀態，不要略過不提。
  3. 不確定使用者是否還想要某件事被處理時，要主動問清楚，不要自行假設對方已經放棄或不需要了。

## 動手前記錄

- **只要是實作項目（寫入/修改任何檔案，不限於`src/`/`scripts/`/`outputs/`，也包含`.claude/`設定、hook腳本、CLAUDE.md本身等），一律要在`progress/`產出記錄，動手前建、完成後補結果段落，沒有例外、不分大小。** 不是「非顯而易見的動作才需要」這種主觀判斷，是「只要有實作就要有記錄」這個客觀標準。
- 記錄檔（檔名格式 `YYYY-MM-DD-主題.md`）內容至少包含：任務、已知限制、本次計畫怎麼做、本輪動作範圍（唯讀查證 vs 允許修改哪些檔案），完成後補上結果段落（做了什麼、依據哪次工具呼叫的實際結果、驗證方式）。
- 這是延續全域 CLAUDE.md 第10條（`_agent_logs/`）的專案內落地方式：`_agent_logs/` 專門記「停損機制觸發」與「回滾檢查點」；`progress/` 記一般性的任務執行過程，兩者互補，不重複。
- **無論使用者當輪有沒有提醒，這條規則都要主動遵守**——不能因為使用者這次沒特別交代就跳過，這是本專案的預設行為，不是使用者要求才啟動的行為。

## 交付檔案 vs 中繼檔案

- 交付成品：`outputs/kb.html`（由 `npm run build` 從 `src/data/*.json` + `src/lib/kbEngine.js` 產生，不手動編輯）。
- 資料來源：`src/data/services.json`、`src/data/templates.json`（人工維護）。
- 修改交付檔案前，若使用者說「僅能回覆/禁止實作」，代表只做唯讀查證與回報，不寫入 `src/`、`scripts/`、`outputs/`，需等待使用者明確說可以動手才能寫入。
